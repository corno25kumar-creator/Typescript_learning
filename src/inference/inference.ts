export function Inference () {

    // type script automatically take the var type so we dont have to type it saprately
    let a = "1"
    console.log(`let a = "12"`)
    console.log('it automatically take the vale of a as String')
}